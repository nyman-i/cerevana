name: PR checks

on:
  pull_request:
    branches: [main]

jobs:
  checks:
    uses: ./.github/workflows/checks.yml
    secrets: inherit
    permissions:
      contents: read
      pull-requests: read
